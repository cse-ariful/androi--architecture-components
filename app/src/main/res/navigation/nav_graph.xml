<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_fragment">
    <fragment
        android:id="@+id/home_fragment"
        android:name="com.example.architecturecomponent.navigation.Home"
        android:label="Home "
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/shop_fragment"
        android:name="com.example.architecturecomponent.navigation.Shop"
        android:label="fragment_shop"
        tools:layout="@layout/fragment_shop" >
        <action
            android:id="@+id/to_about"
            app:destination="@id/about_fragment" />
    </fragment>
    <fragment
        android:id="@+id/about_fragment"
        android:name="com.example.architecturecomponent.navigation.About"
        android:label="About"
        tools:layout="@layout/fragment_about" >
        <argument
            android:name="product_count"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>
    <fragment
        android:id="@+id/cart_fragment"
        android:name="com.example.architecturecomponent.navigation.Cart"
        android:label="Cart"
        tools:layout="@layout/fragment_cart" />
</navigation>